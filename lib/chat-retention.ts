/**
 * チャットの会話記録を何日持つか。
 *
 * ⚠ ここが唯一の出どころ。画面の注記・プライバシーポリシー・掃除の cron が全部これを読む。
 *   数字を直書きすると、日数を変えたときに「90日で消すと書いてあるのに消えていない」
 *   （逆に「30日と言いながら90日持っている」）という状態になる。
 * ⚠ 対象は AI とのやりとり（chat_conversations / chat_messages）だけ。
 *   support_tickets（本人が連絡先付きで送ってきた問い合わせ）はこの日数では消さない。
 *
 * クライアント側（ChatWidget）からも読むので、このファイルにサーバー専用のものを import しない。
 */
export const CHAT_LOG_RETENTION_DAYS = 90
